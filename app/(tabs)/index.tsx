import { Link } from "expo-router";
import { Pressable, Text, TouchableOpacity, View, Image } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "flex-start",
        alignItems: "center",
      }}
    >
      <Text>Native Application</Text>
    </View>
  );
}
